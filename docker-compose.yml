services:
  bc-game-automation:
    build: .
    image: bc-game-automation:local
    env_file:
      - path: .env
        format: raw
    environment:
      BC_GAME_DATA_DIR: /data
    volumes:
      - ./docker-data:/data
    restart: unless-stopped
